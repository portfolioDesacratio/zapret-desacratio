# ═══════════════════════════════════════════════════════════════
# Стратегия: default — SAFE & WORKING
# DESC: fake+badsum на HTTPS, split2 на HTTP, QUIC blocked
# ═══════════════════════════════════════════════════════════════
NFQWS_MODE="nfqws"
NFQWS_QNUM="200"
NFQWS_ARGS="--filter-tcp=80 --dpi-desync=split2 --dpi-desync-autottl=2 --dpi-desync-fooling=md5sig --new --filter-tcp=443 --dpi-desync=fake --dpi-desync-fooling=badsum"
